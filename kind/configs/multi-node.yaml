# kind multi-node cluster config
# Use: kind create cluster --name <name> --config kind/configs/multi-node.yaml
# Requires kind to be installed (see https://kind.sigs.k8s.io/)
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
