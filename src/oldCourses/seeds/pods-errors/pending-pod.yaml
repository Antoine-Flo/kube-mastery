apiVersion: v1
kind: Pod
metadata:
  name: pending-pod
  namespace: default
spec:
  # Impossible node selector so the pod stays Pending
  nodeSelector:
    nonexistent-label: 'must-not-exist'
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.9
status:
  phase: Pending
