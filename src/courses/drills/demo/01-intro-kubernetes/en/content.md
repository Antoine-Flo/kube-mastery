---
description: List cluster nodes with kubectl.
---

# Intro: kubectl and your cluster

Use the terminal to inspect the cluster.

Run `kubectl get nodes` and check that nodes appear.

:::solution
kubectl get nodes
:::
