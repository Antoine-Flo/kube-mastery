import type { LocalModule } from '../types'

export const module: LocalModule = {
  title: {
    en: 'Workloads'
  },
  description: {
    en: 'Deploy apps reliably with Deployments, inject configuration, and add health checks.'
  },
  tags: ['crash-course', 'beginner']
}
