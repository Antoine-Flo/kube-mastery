import type { LocalModule } from '../../types';

export const module: LocalModule = {
    title: {
        en: 'Configuration',
        fr: 'Configuration',
    },
    description: {
        en: 'Learn how to configure applications in Kubernetes: ConfigMaps, Secrets, resource management (requests/limits), probes (liveness, readiness, startup), and kubeconfig',
        fr: 'Apprenez à configurer les applications dans Kubernetes : ConfigMaps, Secrets, gestion des ressources (requests/limits), probes (liveness, readiness, startup), et kubeconfig',
    },
    tags: ['CKA', 'CKAD', 'configuration'],
};
