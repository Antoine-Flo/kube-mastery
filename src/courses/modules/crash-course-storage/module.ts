import type { LocalModule } from '../types'

export const module: LocalModule = {
  title: {
    en: 'Storage'
  },
  description: {
    en: 'Persist data with volumes, PersistentVolumes, and dynamic provisioning.'
  },
  tags: ['crash-course', 'beginner']
}
