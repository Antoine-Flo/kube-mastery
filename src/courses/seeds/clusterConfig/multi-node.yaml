# kind multi-node cluster config
# Shared source for:
# - kind cluster creation (conformance)
# - simulator node bootstrap topology
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
