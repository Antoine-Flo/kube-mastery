apiVersion: v1
kind: Pod
metadata:
  name: imagepull-pod
  namespace: default
  labels:
    app: broken
spec:
  containers:
    - name: broken-container
      image: nonexistent-registry.io/nonexistent-image:latest
