apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: production
  labels:
    app: nginx
    env: production
spec:
  type: ClusterIP
  selector:
    app: nginx
    env: production
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
