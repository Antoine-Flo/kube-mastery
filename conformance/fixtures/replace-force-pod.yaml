apiVersion: v1
kind: Pod
metadata:
  name: replace-target
  namespace: default
spec:
  restartPolicy: Never
  containers:
    - name: main
      image: busybox
      command:
        - sleep
        - "3600"
