apiVersion: v1
kind: Pod
metadata:
  name: annotated-pod
  labels:
    app: web
  annotations:
    contact: 'platform-team@example.com'
    runbook: 'https://wiki.example.com/runbooks/web'
    git-commit: 'a3f2c1d'
spec:
  containers:
    - name: nginx
      image: nginx:1.28